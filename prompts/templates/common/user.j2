---
version: 1.0
description: A template for user interaction handling in financial text processing.
author: Data science team
model: gpt-4.1
---

{% set user_type = user_type | default("extract") %}

{% if user_type == "extract"%}
Extract the required information from the following excerpts and follow all instructions accurately:

{{ chunks | default("N/A") }}

{% endif %}


{% if user_type == "critique"%}
Validate the following model-extracted output:

{{ extraction | default("N/A") }}

For reference, the excerpts (chunks) used to produce the extraction are included below:

{{ chunks | default("N/A") }}

{% endif %}


{% if user_type == "evaluate"%}
Evaluate the stated premise using the provided fields. Follow the instructions and the expected output format exactly.

{{ history | default("N/A") }}

{% endif %}


{% if user_type == "consolidate"%}
Taking into account the evaluated premises, consolidate them into a final assessment. Follow the instructions and the expected output format strictly and exactly.

{{ history | default("N/A") }}

{% endif %}


{% if user_type == "summarize"%}
Provide a concise summary of the following content, focusing on the key points and main ideas:

{{ content | default("N/A") }}

{% endif %}
